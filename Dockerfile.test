# 72Hours Test Runner Dockerfile
FROM python:3.11-alpine

# 安装依赖
RUN apk add --no-cache curl

# 设置工作目录
WORKDIR /app

# 复制测试脚本
COPY test_docker_runner.py .

# 默认命令
CMD ["python3", "test_docker_runner.py"]
